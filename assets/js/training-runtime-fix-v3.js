/* BIOTROP — Runtime fix V3 para Treinamentos */
(() => {
  'use strict';
  if (window.__BIOTROP_TRAINING_RUNTIME_V3__) return;
  window.__BIOTROP_TRAINING_RUNTIME_V3__ = true;
  const wait = (fn, ms = 250, max = 80) => new Promise(resolve => { let n=0; const timer=setInterval(()=>{ n++; try{ if(fn()){clearInterval(timer);resolve(true);return;} }catch(_){} if(n>=max){clearInterval(timer);resolve(false);} },ms); });
  async function loadTrainingData(){const sb=window.SB,prod=window.BIOTROP_PRODUCTION_V2,state=prod?.state,profile=state?.context?.profile;if(!sb||!state||!profile?.id)return false;const courses=await sb.from('training_courses').select('*').eq('active',true).order('created_at',{ascending:false});if(courses.error)throw courses.error;const progress=await sb.from('training_progress').select('id,training_id,user_id,percentage,completed_at,updated_at').eq('user_id',profile.id);if(progress.error)throw progress.error;state.trainings=courses.data||[];state.trainingProgress=progress.data||[];return true}
  function repairBindings(){if(typeof window.attachTrainingEvents==='function')window.attachTrainingAreaEvents=window.attachTrainingEvents}
  function loadV4(){if(document.querySelector('script[data-training-academy-v4]'))return;const s=document.createElement('script');s.src='./assets/js/training-academy-v4.js?v=20260901';s.async=false;s.dataset.trainingAcademyV4='1';s.onload=()=>{repairBindings();setTimeout(()=>{const a=window.BIOTROP_PRODUCTION_V2?.state?.currentArea;if(a==='treinamentos'&&typeof window.navigateTo==='function')window.navigateTo('treinamentos')},700)};s.onerror=()=>console.error('[BIOTROP] Falha ao carregar training-academy-v4.js');document.body.appendChild(s)}
  async function boot(){const ready=await wait(()=>Boolean(window.SB&&window.BIOTROP_PRODUCTION_V2?.state?.context?.profile?.id&&typeof window.renderTrainingArea==='function'));if(!ready)return;repairBindings();try{await loadTrainingData();loadV4();repairBindings();const a=window.BIOTROP_PRODUCTION_V2?.state?.currentArea||window.BIOTROP_PRODUCTION_V2?.state?.currentPage||window.STATE?.currentArea;if(a==='treinamentos'&&typeof window.navigateTo==='function')setTimeout(()=>window.navigateTo('treinamentos'),900)}catch(error){console.error('[BIOTROP][Treinamentos V3]',error);if(typeof window.notify==='function')window.notify('Não foi possível carregar os treinamentos: '+(error.message||error),'error')}}
  window.addEventListener('biotrop:refresh',boot);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
